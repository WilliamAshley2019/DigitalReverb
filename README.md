This is still in build as part of a larger multifxs muse.  The next build will likely add motorola dsp core emulation to a closer extent the m56x. 
This plugin took quite a bit of work to a resize issue that took all day to fix. This is the alpha version its only slightly been tested so 
bugs are likely to still exist. 

Built in JUCE 8.0.12  add DSP module to audio basics.
Compiled through Visual Studio 2026. 

Tested on Windows 11 25h2 in FL Studio 2025.2.3
